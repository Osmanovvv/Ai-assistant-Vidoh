import { sql, type SQL } from 'drizzle-orm';

import { aiCalls } from '../../db/schema.js';

/**
 * «Вызовов без известной цены» — **одним условием на всех**.
 *
 * Число отвечает на один вопрос: сколько обращений к модели мы не смогли
 * оценить, то есть насколько показанная сумма ниже настоящей. Рядом с
 * ним панель печатает оговорку «расход не меньше показанного», и ровно
 * поэтому в него нельзя пускать **сорвавшиеся** вызовы: у отказа цены
 * нет и быть не может — 403, таймаут и обрыв не тарифицируются. Отказы
 * считаются своим числом (`failed`) в тех же запросах, так что без
 * фильтра один и тот же вызов попадал в оба разом.
 *
 * Цена ошибки не в лишней единице. Оговорка, которая горит после
 * **любого** сбоя модели, перестаёт значить что-либо, а настоящий
 * случай — модели нет в прайс-листе — от неё уже не отличить. Заказчица
 * читает «расход выше показанного» и не может узнать, правда это или
 * след вчерашнего таймаута; по этому же числу она решает про цену
 * подписки.
 *
 * **Условие живёт здесь, потому что его забывали дважды.** Первый раз —
 * в `account-spend.ts`, и там об этом написано своими словами: «сперва он
 * был забыт, и одно и то же число считалось двумя разными способами».
 * Починили одно место, а рядом остались пять: обзор, строка человека,
 * расход человека для §10.5 и два разреза раздела расходов. Значит дело
 * не в невнимательности, а в том, что условие было переписываемым. Теперь
 * оно одно, и его нельзя обойти незаметно — за этим стоит страж
 * `unpriced.test.ts`.
 */
export function unpricedSql(): SQL {
  return sql`(${aiCalls.costMicros} is null and ${paidSql()})`;
}

/**
 * «За отправку платили» — тоже одним условием на всех.
 *
 * Не только удавшиеся (ревизия этапов 1–2). У отказа цены обычно нет и
 * быть не может — 403, таймаут и обрыв не тарифицируются. Но
 * распознавание платит **отправкой**: если звук приняли, а результат не
 * доехал, секунды списаны, и с этой ревизии они доезжают до учёта. У
 * такой строки объём есть, а цена может быть неизвестна — модели нет в
 * прайсе, — и молчать о ней нельзя: панель сказала бы «посчитано точно»
 * там, где деньги ушли мимо счёта. Ноль вместо «не смогли» — та же ложь.
 *
 * Признак оплаченности — заполненный объём, а не признак успеха: пустой
 * объём у отказа значит «платить было не за что».
 *
 * **Отдельной функцией, потому что его тоже переписали дважды** (ревизия
 * этапов 1–2, дефект №11). Оно нужно не только «вызовам без цены»:
 * себестоимость выгрузки берёт из учёта ровно те строки, за которые
 * платили, — и писала это условие своими словами через drizzle. Сперва
 * два написания расходились по смыслу: скрипт брал всё подряд, и один
 * сорвавшийся вызов делал модель «без цены» и выбрасывал выгрузку из
 * средней — той самой, по которой назначается цена подписки. Потом их
 * дважды подтягивали друг к другу руками — по признаку успеха, затем по
 * объёму, — и оба раза двумя написаниями, которые сверить было нечем:
 * следующая правка одного разошлась бы с другим молча. Страж в
 * `unpriced.test.ts` держит и это написание.
 */
export function paidSql(): SQL {
  return sql`(
    ${aiCalls.ok}
    or ${aiCalls.audioSeconds} is not null
    or ${aiCalls.tokensIn} is not null
  )`;
}

/** То же число готовым счётчиком: `count(*) filter (...)` для select. */
export function unpricedCountSql(): SQL<number> {
  return sql<number>`count(*) filter (where ${unpricedSql()})::int`;
}
