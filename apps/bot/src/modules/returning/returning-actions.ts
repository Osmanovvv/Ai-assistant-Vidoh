/**
 * Кнопки экрана возвращения (§13.6 ТЗ).
 *
 * Живут отдельно от обработчика по той же причине, что и остальные:
 * спрашивает конвейер, отвечает обработчик, и разъехавшиеся префиксы
 * сломали бы половину нажатий.
 */
export const RETURNING_ACTION = {
  /** «Продолжить старое» — актуальные записи и ближайшие шаги. */
  keep: 'back:keep',
  /** «Начать с чистого листа» — старое уходит в фон, не удаляется. */
  fresh: 'back:fresh',
} as const;
