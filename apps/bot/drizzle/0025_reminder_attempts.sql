-- Счётчик неудачных отправок (§5 ТЗ, колонка attempts у reminders).
--
-- Без него неудачная отправка помечалась отправленной: сообщение терялось
-- молча, а человек, ничего не получивший, попадал в серию молчания (3.17)
-- — и продукт снижал ему частоту за собственный сбой.
ALTER TABLE "reminders" ADD COLUMN "attempts" integer DEFAULT 0 NOT NULL;
