-- Режим правки у отложенного вопроса (задача 3.82).
--
-- §7.4 различает дополнение и замену: «а ещё туда надо взять карту
-- прививок» дописывает подробность, «нет, лучше в пятницу» переписывает
-- поле. Резолвер это различие возвращает, и разбор его соблюдает.
--
-- А вопрос — не соблюдал: в таблице режима не было вовсе, и оба ответа,
-- кнопкой и голосом, применялись как замена. Значит подробность из
-- вопроса выбрасывалась: менять оказывалось нечего, человек получал
-- «Добавила к прошлой», а в записи не появлялось ничего.
--
-- Пусто означает «замена» — так вело себя применение по умолчанию, и
-- прежние открытые вопросы должны дожить с прежним поведением.
ALTER TABLE "pending_questions" ADD COLUMN "mode" text;
