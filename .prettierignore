node_modules/
dist/
coverage/
.data/
docs/
package-lock.json
playwright-report/
test-results/

# Пишет drizzle-kit, а не человек: перевод строки там CRLF, и prettier с
# ним не согласен. Форматировать бесполезно — следующая же миграция
# перепишет файл по-своему, и общая проверка снова покраснеет.
#
# Найдено 03.09.2026: `npm run check` падал на этом файле, то есть та
# самая команда, которая должна доказывать «всё зелено», была красной с
# 83306fd — и до тестов дело не доходило вовсе.
apps/bot/drizzle/meta/
